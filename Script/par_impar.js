// let array=[1,2,3,4,5,6,7,9,10];
function isPair(arr){
    // if(arr.length % 2  ){
    //     console.log('el array es inpar ')
    // }else{
    //  return console.log('true');
    // }
    ////////////////////////////////////////////////
    //para facilitar todo el codigo 
    return !(arr.length % 2);
}
function isOdd(arr){ //es impar
    return (arr.length % 2);
}
// isPair(array);